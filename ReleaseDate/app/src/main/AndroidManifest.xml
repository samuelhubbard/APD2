<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.samuelhubbard.android.releasedate">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TrackedGamesActivity"
            android:launchMode="singleTop" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".UpcomingGamesActivity"
            android:label="@string/upcoming_activity_title"
            android:parentActivityName=".TrackedGamesActivity">

        </activity>

        <activity android:name=".AboutAppActivity"
            android:label="@string/about_activity_title"
            android:parentActivityName=".TrackedGamesActivity">

        </activity>

        <activity android:name=".UpcomingGameDetailActivity"
            android:label="@string/game_detail_activity_title"
            android:parentActivityName=".UpcomingGamesActivity">

        </activity>
    </application>

</manifest>
